# RC
This is a project in C++ that develops a prototype of a centralized messaging service, with a User Application (User) and a Directory Server (DS) that runs on a machine with known IP addresses and ports.
